"""Format conversion / verification helpers (markitdown, mermaid).

Submodules:
- ``markitdown`` — thin wrapper over the ``markitdown`` package (file → markdown).
- ``mermaid``    — Mermaid diagram verifier + bounded LLM repair loop.
"""
