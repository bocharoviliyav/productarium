# Task: document the {spec_kind} specification "{artifact_name}"

Below are the deterministic skeleton (rendered from the parsed spec) and the raw spec content. Produce the complete human-readable documentation following your system instructions.

## Skeleton (ground truth — every endpoint/channel/schema is listed)

<skeleton>
{skeleton}
</skeleton>

## Raw spec content

<spec_content>
{content}
</spec_content>

## Reminders

- Use `spec_lookup` to pull exact fragments for the elements you document in detail.
- Your FINAL message must contain ONLY the finished Markdown document.
